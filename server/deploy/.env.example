# Copy to .env on the authorized host; this placeholder intentionally fails.
DUEL_PUBLIC_HOST=replace-with-your-domain.invalid
# Required: choose owned-server policy in milliseconds, 1000..604800000.
# No claim this value is recovered original chest timing. 3000 is local testing.
DUEL_CHEST_UNLOCK_MS=
# Before release, replace moving tags with reviewed image@sha256:... digests.
NODE_IMAGE=node:24-bookworm-slim
CADDY_IMAGE=caddy:2-alpine
# No passwords, session tokens, device link codes or seed accounts belong here.
